version: 2

orbs:
  miyajan:
    executors:
      default:
        docker:
          - image: circleci/ruby:2.4.4
    commands:
      sayhello:
        description: "Hello!"
        parameters:
          to:
            type: string
            default: "World"
        steps:
          - run: echo "Hello << parameters.to >>"
    jobs:
      hello:
        executor: default
        parameters:
          to:
            type: string
            default: "World"
        steps:
          - sayhello:
              to: << parameters.to >>

jobs:
  sayhello:
    parameters:
      what:
        default: "World"
        type: string
    docker:
      - image: circleci/ruby:2.4.4
    steps:
      - run: echo "Hello << parameters.what >>"

workflows:
  version: 2
  main:
    jobs:
      - sayhello

